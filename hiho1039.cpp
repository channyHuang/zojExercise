#include <iostream>
#include <cstring>
#include <string>

int main() {
	int numOfCase;
	std::string str;
	std::cin >> numOfCase;
	while (numOfCase--) {
		std::cin >> str;
		int len = str.length();
		for (int i = 0; i < len; i++) {
			
		}

		std::cout << std::endl;
	}
}
